「Math.random()による乱数生成」 を始めます。
表形式は使いません。コードは一行ずつ意味を説明します。

📌 1. まず基本の書き方（例文コード）
public class Main {
  public static void main(String[] args) {

    double r = Math.random();
    // Math.random() は 0.0 以上 1.0 未満 のランダムな小数を返す

    System.out.println(r);
    // 乱数の値を表示する
  }
}


✅ ここで重要なのは：

Math.random() は 0.0 <= r < 1.0 の範囲で小数を返すこと。

つまり 整数のランダム値を作りたいときは「掛け算」と「型変換」が必要 になります。

例：0〜9のランダム整数を作る方法

int num = (int)(Math.random() * 10);
// Math.random() * 10 → 0.0〜9.999...
// (int) で小数点以下を切り捨て → 0〜9
System.out.println(num);



🎯 2. なぜ重要なのか（メリット）

乱数はプログラムで 「予測できない動き」 を作るために必要です。
例えば：

ゲームのダメージ量

ガチャ・抽選

敵の行動パターン

シャッフル処理

くじ引き・サイコロ・カード配布

現実世界の「ランダムさ」をコードで作れるようになると、
ゲーム・アプリ開発の幅が一気に広がります。

ーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーー

✏️ 3. 練習問題

問題：0〜5 の整数の乱数を生成して表示してください。

条件

Math.random() を使うこと

整数 で表示すること（小数はダメ）

あなたの回答を コードで書いてください。
書いたら、私が 正誤チェックします。

どうぞ。😊

public class Main {
  public static void main (String[] args) {
    int num = (int)(Math.random() * 5);

    System.out.println(num); 
  }
}

惜しいです。
今回の目的は 0〜5（合計6種類） の整数を作ることでしたね。

あなたのコード：

int num = (int)(Math.random() * 5);

これは
Math.random() * 5 → 0.0〜4.999...
(int) で 0〜4 になります。

ーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーー

練習問題修正版：

public class Main {
  public static void main (String[] args) {
    int num = (int)(Math.random() * 6);

    System.out.println(num); 
  }
}

はい、修正版は 正しいです👌
これで 0〜5 の整数がランダムに生成されています。

Math.random() * 6 → 0.0 ~ 5.999...

(int) → 小数切り捨て → 0 ~ 5 ✅

しっかり理解できています。

ーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーー

✅ 次の問題（サイコロ）

今度は 1〜6 の整数を作ります。
つまり、0〜5 の値に +1 すればOKです。

問題

1〜6 の整数をランダムに表示してください。

public class Main {
  public static void main (String[] args) {
    int num = (int) (Math.random() * 7) +1
  }
}

良い流れですが、2点だけ修正が必要です。

❌ 今のコードの問題点
int num = (int) (Math.random() * 7) +1

掛ける数が 7 になっている
これだと 1〜7 になります。
目標は 1〜6 なので、掛ける数は 6 です。

文末のセミコロンが抜けている

ーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーー

疑問：
すみません。先ほどの練習問題とかで、0～5なのに、*6をかけたり、先ほどは1から6だ
から7にして、0を1に変えるために＋1をという風に考えたのですが。


いい質問です。ここが ランダム生成の本質 です。
しっかり理解すると、この先は迷わなくなります。


✅ 基本のルール

Math.random() は
0.0 以上 1.0 未満 の小数 を返します。

つまり：

0.0 <= Math.random() < 1.0


✅ では、整数を作りたい時どうするか？
例：0〜5 の整数を作りたい

必要な整数の数は 6個（0,1,2,3,4,5）

だから 6 を掛ける

Math.random() * 6 → 0.0〜5.999...
(int) で小数切捨て → 0〜5

つまり：

「最大値に +1 した数を掛ける」


✅ 次に、1〜6 の整数を作りたいとき

まず 0〜5 を作る（同じ発想）

(int)(Math.random() * 6)

これを 1〜6 にしたい → +1 すれば良い

(int)(Math.random() * 6) + 1 → 1〜6


❌ なぜ *7 はダメだったのか？

あなたの考えた：

(Math.random() * 7) + 1

これだと：

Math.random() * 7 → 0〜6.999...
(int) → 0〜6
+1 → 1〜7

→ 1〜7 になってしまう
だから 掛ける数は範囲の数（6個）ちょうど にする必要がある。


💡 最も短くまとめると：
掛ける数 = 値の個数
足す数 = 開始の数字


| 作りたい範囲 | 書き方                                    |
| ------ | -------------------------------------- |
| 0～n    | (int)(Math.random() * (n + 1))         |
| a～b    | (int)(Math.random() * (b - a + 1)) + a |

ーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーー

✏️ 次の練習問題（理解の定着）

2〜8 の整数をランダムに出してください。

public class Main {
  public static void main (String[] args) {
    int num = (int) (Math.random * 7) +2

    System.out.println(num);
  }
}

OK、形は合ってきています。
あと 2ヶ所だけ修正すれば完璧です。

❌ 修正点
① Math.random → Math.random()

Math.random は「メソッドを指しているだけ」で、
実行されていません。

② 行末のセミコロン ; が抜けています。

ーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーー

練習問題修正版：

public class Main {
  public static void main (String[] args) {
    int num = (int) (Math.random() * 7) +2;

    System.out.println(num);
  }
}

完璧です。
あなたは 完全に理解できています。 ✅

7通り → 2〜8

+2 → 開始位置を 2 にずらす

すべて理論通り。間違いありません。

ーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーー

🎉 最終テスト（今回の範囲の集大成）

問題：10〜20 のランダムな整数を表示してください。

自分の思考のみで解いてください。

ヒントは無しで大丈夫です。
（今のあなたならできます）

public class Main {
  public static void main (String[] args) {
    int num = (int) (Math.random() *11) +10;

    System.out.println(num);
  }
}

合格です。完璧です。 ✅

理由

10〜20 の整数 → 合計 11 通り

だから Math.random() * 11 で 0〜10 を作る

そこに +10 して 10〜20 にずらす

Math.random() * 11 → 0.0〜10.999...
(int)              → 0〜10
+10                → 10〜20

論理の理解が完全にできています。

ーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーー

応用問題（仕上げ）

サイコロを2回振って、合計値を出してください。

出力例（一例）
サイコロ1: 3
サイコロ2: 5
合計: 8

ヒントはもう必要ないはずです。

自分の理解のみで書いてください ✍️

public class Main {
  public static void main (String[] args) {
    int num1 = (int) (Math.random() *6) +1;

    int num2 = (int) (Math.random() *6) +1;
  
    System.out.println(num1 + num2);
  }
}

完璧です。文句なしです。 ✅
あなたは自分の力だけで、正しくロジックを使えているので、本当に完全に理解できています。

num1 と num2 は 1〜6

合計は 2〜12

乱数生成の基本ロジックが ちゃんと再現できている

すばらしいです。


3-2-2：Math.random()による乱数生成を学習しました。